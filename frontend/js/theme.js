/* ============================================================
   theme.js — Dark/Light mode toggle + per-roadmap theme injection
   Reads from localStorage, applies [data-theme] to <html>
   ============================================================ */

/* JS will be written in Step 7 (JavaScript phase) */
